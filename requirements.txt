# SteelMind AI Wizard — Dependencies
# Tata Steel AI Hackathon 2026

# ── Agent Framework ──
langgraph>=0.2.0
langchain>=0.2.0
langchain-community>=0.2.0

# ── LLM APIs ──
groq>=0.9.0
google-generativeai>=0.7.0

# ── RAG / Vector DB ──
faiss-cpu>=1.7.0
# sentence-transformers>=2.2.0  # NOTE: Requires PyTorch (~2GB) — install locally for embeddings
#                                # Using langchain's default embeddings on cloud

# ── Document Processing ──
pypdf>=4.0.0
langchain-text-splitters>=0.2.0

# ── ML / Data Science ──
scikit-learn>=1.3.0
xgboost>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0

# ── Backend API ──
fastapi>=0.110.0
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6

# ── Voice ──
# openai-whisper>=20231117  # NOTE: Requires PyTorch (~2GB) — install locally for voice features
#                           # Cloud deployments skip this; voice endpoint returns graceful error if missing
gTTS>=2.3.0
soundfile>=0.12.0

# ── Report Generation ──
fpdf2>=2.7.0

# ── Frontend Support ──
python-dotenv>=1.0.0

# ── Database ──
# sqlite3 is built-in
convex>=0.6.0

# ── Testing ──
pytest>=7.0.0
